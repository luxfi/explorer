# LUX P-Chain (Platform Chain) Explorer - Testnet
# Validators, Staking, Networks
# API: api-pchain-test.lux.network -> localhost:4110

services:
  pchain-indexer:
    build:
      context: ../indexers
      args:
        CHAIN: pchain
    image: luxfi/pchain-indexer:latest
    restart: always
    container_name: 'lux-pchain-testnet-indexer'
    ports:
      - "4110:4000"
    networks:
      - hanzo-network
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      RPC_ENDPOINT: http://host.docker.internal:9630/ext/bc/P
      DATABASE_URL: postgresql://blockscout:blockscout@lux-postgres:5432/explorer_pchain_test?sslmode=disable
      HTTP_PORT: 4000
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:4000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

networks:
  hanzo-network:
    external: true
