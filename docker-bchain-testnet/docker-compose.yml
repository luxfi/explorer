# LUX B-Chain (Bridge Chain) Explorer - Testnet
# MPC Signers, Cross-chain Bridges, Epochs
# API: api-bchain-test.lux.network -> localhost:4610

services:
  bchain-indexer:
    build:
      context: ../indexers
      args:
        CHAIN: bchain
    image: luxfi/bchain-indexer:latest
    restart: always
    container_name: 'lux-bchain-testnet-indexer'
    ports:
      - "4610:4000"
    networks:
      - hanzo-network
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      RPC_ENDPOINT: http://host.docker.internal:9630/ext/bc/B
      DATABASE_URL: postgresql://blockscout:blockscout@lux-postgres:5432/explorer_bchain_test?sslmode=disable
      HTTP_PORT: 4000
      NETWORK: testnet
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:4000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

networks:
  hanzo-network:
    external: true
