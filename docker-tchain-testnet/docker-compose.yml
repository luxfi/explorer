# LUX T-Chain (Teleport Chain) Explorer - Testnet
# Cross-chain Messages, Warp Signatures, Transfers
# API: api-tchain-test.lux.network -> localhost:4710

services:
  tchain-indexer:
    build:
      context: ../indexers
      args:
        CHAIN: tchain
    image: luxfi/tchain-indexer:latest
    restart: always
    container_name: 'lux-tchain-testnet-indexer'
    ports:
      - "4710:4000"
    networks:
      - hanzo-network
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      RPC_ENDPOINT: http://host.docker.internal:9630/ext/bc/T
      DATABASE_URL: postgresql://blockscout:blockscout@lux-postgres:5432/explorer_tchain_test?sslmode=disable
      HTTP_PORT: 4000
      NETWORK: testnet
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:4000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

networks:
  hanzo-network:
    external: true
