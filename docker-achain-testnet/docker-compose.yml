# LUX A-Chain (AI Chain) Explorer - Testnet
# AI Providers, Tasks, Attestations, Rewards
# API: api-achain-test.lux.network -> localhost:4510

services:
  achain-indexer:
    build:
      context: ../indexers
      args:
        CHAIN: achain
    image: luxfi/achain-indexer:latest
    restart: always
    container_name: 'lux-achain-testnet-indexer'
    ports:
      - "4510:4000"
    networks:
      - hanzo-network
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      RPC_ENDPOINT: http://host.docker.internal:9630/ext/bc/A
      DATABASE_URL: postgresql://blockscout:blockscout@lux-postgres:5432/explorer_achain_test?sslmode=disable
      HTTP_PORT: 4000
      NETWORK: testnet
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:4000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

networks:
  hanzo-network:
    external: true
