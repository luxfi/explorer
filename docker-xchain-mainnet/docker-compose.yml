# LUX X-Chain (Exchange Chain) Explorer - Mainnet
# Assets, UTXOs, Transfers
# API: api-xchain.lux.network -> localhost:4200

services:
  xchain-indexer:
    build:
      context: ../indexers
      args:
        CHAIN: xchain
    image: luxfi/xchain-indexer:latest
    restart: always
    container_name: 'lux-xchain-mainnet-indexer'
    ports:
      - "4200:4000"
    networks:
      - hanzo-network
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      RPC_ENDPOINT: http://host.docker.internal:9630/ext/bc/X
      DATABASE_URL: postgresql://blockscout:blockscout@lux-postgres:5432/explorer_xchain?sslmode=disable
      HTTP_PORT: 4000
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:4000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

networks:
  hanzo-network:
    external: true
